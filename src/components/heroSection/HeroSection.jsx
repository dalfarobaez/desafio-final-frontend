import MainWrapper from '../layout/MainWrapper';
import { HeroImageStyled } from './HeroSection.styles';

const HeroSection = () => {
  return (
    <MainWrapper>
      <HeroImageStyled />
    </MainWrapper>
  );
};

export default HeroSection;
